project_path = $(save_path:/work/vfelso)/planning-depth-differences
executable = $(project_path)/env/bin/python
filename = $(project_path)/analysis/methods/static/src/plot_bms.py
arguments = $(filename) -e $(experiment:MainExperiment) -s $(subdirectory:methods/static)
output = $(project_path)/analysis/$(subdirectory:methods/static)/log/BMS_$(experiment:MainExperiment).stdout
error  = log/BMS_$(experiment:MainExperiment).stderr
log   = log/BMS_$(experiment:MainExperiment).log
request_memory = 16000
request_cpus = 1

queue
